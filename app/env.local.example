# Local Development Environment Configuration
# Copy this to .env.local and run ./scripts/setup-local-cognito.sh to set up cognito-local

# AWS Cognito Configuration (cognito-local)
# These will be populated by setup-local-cognito.sh
VITE_COGNITO_USER_POOL_ID=us-east-1_example
VITE_COGNITO_USER_POOL_CLIENT_ID=example_client
VITE_COGNITO_IDENTITY_POOL_ID=us-east-1:00000000-0000-0000-0000-000000000000
VITE_COGNITO_ENDPOINT=http://localhost:9229

# API Configuration (Local SAM)
VITE_API_BASE_URL=http://localhost:3000

# Environment
NODE_ENV=development
VITE_ENVIRONMENT=local

# AWS Region
VITE_AWS_REGION=us-east-1

# Local AWS Credentials for cognito-local
VITE_AWS_ACCESS_KEY_ID=local
VITE_AWS_SECRET_ACCESS_KEY=local

# Test User Credentials (created by setup script)
VITE_TEST_USER_EMAIL=test@valthera.com
VITE_TEST_USER_PASSWORD=TestPass123! 