services:
  camera:
    privileged: true
    runtime: nvidia
    devices:
      - "/dev/bus/usb:/dev/bus/usb"
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility,video
    ports:
      - "8000:8000"

